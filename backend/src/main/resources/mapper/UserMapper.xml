<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.earthseaedu.backend.mapper.UserMapper">

    <sql id="BaseColumns">
        id, mobile, mobile_verified, password_hash, nickname, avatar_url, sex, province, city, country,
        status, is_temp_wechat_user, is_teacher, create_time, update_time, delete_flag
    </sql>

    <select id="findActiveById" resultType="com.earthseaedu.backend.model.auth.User">
        SELECT <include refid="BaseColumns"/>
        FROM users
        WHERE id = #{userId}
          AND delete_flag = '1'
        LIMIT 1
    </select>

    <select id="findActiveByMobile" resultType="com.earthseaedu.backend.model.auth.User">
        SELECT <include refid="BaseColumns"/>
        FROM users
        WHERE mobile = #{mobile}
          AND delete_flag = '1'
        LIMIT 1
    </select>

    <select id="countByNicknameExcludingUserId" resultType="long">
        SELECT COUNT(1)
        FROM users
        WHERE nickname = #{nickname}
          AND delete_flag = '1'
          AND id &lt;&gt; #{userId}
    </select>

    <insert id="insert" parameterType="com.earthseaedu.backend.model.auth.User">
        INSERT INTO users (
            id, mobile, mobile_verified, password_hash, nickname, avatar_url, sex, province, city, country,
            status, is_temp_wechat_user, is_teacher, create_time, update_time, delete_flag
        ) VALUES (
            #{id}, #{mobile}, #{mobileVerified}, #{passwordHash}, #{nickname}, #{avatarUrl}, #{sex}, #{province}, #{city}, #{country},
            #{status}, #{isTempWechatUser}, #{isTeacher}, #{createTime}, #{updateTime}, #{deleteFlag}
        )
    </insert>

    <update id="update" parameterType="com.earthseaedu.backend.model.auth.User">
        UPDATE users
        SET mobile = #{mobile},
            mobile_verified = #{mobileVerified},
            password_hash = #{passwordHash},
            nickname = #{nickname},
            avatar_url = #{avatarUrl},
            sex = #{sex},
            province = #{province},
            city = #{city},
            country = #{country},
            status = #{status},
            is_temp_wechat_user = #{isTempWechatUser},
            is_teacher = #{isTeacher},
            update_time = #{updateTime},
            delete_flag = #{deleteFlag}
        WHERE id = #{id}
    </update>

</mapper>
