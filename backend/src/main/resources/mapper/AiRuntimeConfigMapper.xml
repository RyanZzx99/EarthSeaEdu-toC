<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.earthseaedu.backend.mapper.AiRuntimeConfigMapper">

    <sql id="BaseColumns">
        id, config_group, config_key, config_name, config_value, value_type, is_secret, status, sort_order, remark,
        created_by, updated_by, create_time, update_time, delete_flag
    </sql>

    <select id="listActive" resultType="com.earthseaedu.backend.model.ai.AiRuntimeConfig">
        SELECT <include refid="BaseColumns"/>
        FROM ai_runtime_configs
        WHERE delete_flag = '1'
        ORDER BY config_group ASC, sort_order ASC, id ASC
    </select>

    <select id="findActiveByConfigKey" resultType="com.earthseaedu.backend.model.ai.AiRuntimeConfig">
        SELECT <include refid="BaseColumns"/>
        FROM ai_runtime_configs
        WHERE config_key = #{configKey}
          AND delete_flag = '1'
        LIMIT 1
    </select>

    <insert id="insert" parameterType="com.earthseaedu.backend.model.ai.AiRuntimeConfig" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_runtime_configs (
            config_group, config_key, config_name, config_value, value_type, is_secret, status, sort_order, remark,
            created_by, updated_by, create_time, update_time, delete_flag
        ) VALUES (
            #{configGroup}, #{configKey}, #{configName}, #{configValue}, #{valueType}, #{isSecret}, #{status}, #{sortOrder}, #{remark},
            #{createdBy}, #{updatedBy}, #{createTime}, #{updateTime}, #{deleteFlag}
        )
    </insert>

    <update id="update" parameterType="com.earthseaedu.backend.model.ai.AiRuntimeConfig">
        UPDATE ai_runtime_configs
        SET config_group = #{configGroup},
            config_name = #{configName},
            config_value = #{configValue},
            value_type = #{valueType},
            is_secret = #{isSecret},
            status = #{status},
            sort_order = #{sortOrder},
            remark = #{remark},
            updated_by = #{updatedBy},
            update_time = #{updateTime},
            delete_flag = #{deleteFlag}
        WHERE id = #{id}
    </update>

    <select id="findLatestRuntimeConfig" resultType="map">
        SELECT config_value, status
        FROM ai_runtime_configs
        WHERE config_key = #{configKey}
          AND delete_flag = '1'
        ORDER BY update_time DESC, id DESC
        LIMIT 1
    </select>

</mapper>
